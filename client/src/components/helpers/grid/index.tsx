import * as Column from './components/column';
import { createSlide, createGrid } from './utils/helpers';

const MyGridLibrary = {
    Column,
    createSlide,
    createGrid,
};

export default MyGridLibrary;
